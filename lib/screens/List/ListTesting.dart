import 'package:flutter/material.dart';

class ListTesting extends StatelessWidget {
  const ListTesting({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('hehe'),),
          body: Center(
        child: Text(
          'test',
        ),
      ),
    );
  }
}
